# Rename this file to .env (copy or move) and fill in the keys below.
# The real .env is gitignored and must not be committed.
#
# Example:
#   copy .env_sample .env
# then edit .env and replace the placeholders with your keys.
#
# OPENAI_API_KEY  — transcription / editing (OpenAI)
# ANTHROPIC_API_KEY — book OCR / Claude vision (Anthropic)

OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
