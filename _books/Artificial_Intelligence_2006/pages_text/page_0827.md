<!-- source: Russell_Norvig_AI_2006.djvu page 827 of 1409; embedded text layer, no OCR -->

## Page 827

Глава 17. Принятие сложных решений
827
движется), поэтому в пределе повторное применение функции сжатия всегда
приводит к достижению фиксированной точки.
Теперь предположим, что обновление Беллмана (уравнение 17.6)
рассматривается как оператор в, который используется для одновременного обновления значений
полезности каждого состояния. Обозначим через С7± вектор полезностей для всех
состояний в i-й итерации. В таком случае уравнение обновления Беллмана может быть
записано следующим образом:
Ui+1 <- BUi
Затем нужно найти способ измерения расстояний между векторами полезностей.
Мы будем использовать ^ нормализованный максимум, который измеряет длину
вектора по длине его максимального компонента, следующим образом:
| |С7| | = max \U(s) \
s
При использовании этого определения "расстояние" между двумя векторами,
| \и-и* | |, представляет собой максимальную разность между двумя
соответствующими элементами. Основным математическим результатом данного раздела
является такое утверждение: СГ5 допустим, что С7± и С7±' — два вектора полезностей. В
таком случае получим следующее:
| \BUi-BUi1 | | < Y I \Ui-Ui' || A7.7)
Это означает, что обновление Беллмана представляет собой функцию сжатия на
коэффициент у, применяемую к пространству векторов полезностей. Таким образом,
процедура итерации по значениям всегда сходится к уникальному решению
уравнений Беллмана.
В частности, можно заменить значение С7±' в уравнении 17.7 истинными полезностя-
ми и, для которых в и= U. В таком случае будет получено следующее неравенство:
| |BC7i-C7|| < Y \\Ui-U\\
Поэтому, если | | Ui-u\ | рассматривается как ошибка в оценке с/±э то можно
видеть, что эта ошибка уменьшается при каждой итерации на коэффициент, по
меньшей мере равный у. Это означает, что процедура итерации по значениям сходится
экспоненциально быстро. Можно легко рассчитать количество итераций, требуемых
для достижения заданной предельной ошибки 8, как описано ниже. Вначале
напомним, что, как показывает уравнение 17.1, полезности всех состояний ограничены
значением ±Rmayi/ A-у) • Из этого следует, что максимальная начальная ошибка
определяется соотношением | | U0-u\ | <2^тах/ A-у). Предположим, что для
достижения ошибки, не превышающей 8, выполнено N итераций. В таком случае
потребуется У*'2#тах/ A-у)<8 итераций, поскольку ошибка уменьшается каждый раз по
меньшей мере на величину у. Взяв логарифмы от этого выражения, можно
определить, что достаточно применить следующее количество итераций:
N= Г1одBЯтах/еA-у))/1одA/уI
На рис. 17.4, б показано, как количество итераций N изменяется в зависимости от
у при различных значениях отношения г/Я^*. Положительной особенностью этого
соотношения является то, что из-за экспоненциально быстрой сходимости значение
